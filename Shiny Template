# WORKING WITH SHINY IN R (TEMPLATE)
# CREDIT DATA SCIENCE TUTORIALS 

# Load packages
library(shiny)
library(shinydashboard)

shinyUI(
  dashboardPage(
    dashboardHeader(),
    dashboardSidebar(),
    dashboardBody()
  )
)

shinyServer(function(input, output){

)}
